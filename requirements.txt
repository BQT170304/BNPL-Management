# Runtime dependencies (mirror of [project.dependencies] in pyproject.toml).
# Install with: pip install -r requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
boto3>=1.34
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
aiosqlite>=0.19
alembic>=1.13
pandas>=2.2
matplotlib>=3.8
python-multipart>=0.0.5

# Optional: Prophet cash-flow forecasting (heavy Stan toolchain).
# Without it the app falls back to the built-in naive forecaster.
# Install with: pip install -r requirements-forecast.txt
